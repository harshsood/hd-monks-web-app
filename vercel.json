{
  "version": 2,
  "buildCommand": "npm --prefix frontend install --legacy-peer-deps && npm --prefix frontend run build",
  "outputDirectory": "frontend/build",
  "env": {
    "REACT_APP_BACKEND_URL": "https://hd-monks-web-app.onrender.com"
  }
}
